# pnpm configuration for KodKong monorepo
# Fixes Prisma Client hoisting issues

# Use public-hoist-pattern to hoist Prisma to root
public-hoist-pattern[]=@prisma/*
public-hoist-pattern[]=prisma

# Ensure proper hoisting for all packages
shamefully-hoist=true

# Auto install peers
auto-install-peers=true
